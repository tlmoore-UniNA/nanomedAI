db <- tabItem(tabName = "db",
    navbarPage("The nanomedAI Database",
        tabPanel("Explore & download", icon = icon("database"),
            h2("Some text here")
        )
    ) # End navbarPage
) # End tabItem
